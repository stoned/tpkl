# Pkl command defaults to `pkl` if PKL_EXEC is empty
env PKL_EXEC=
exec tpkl eval module.pkl

env PKL_EXEC=XXXno-such-executable
! exec tpkl eval module.pkl
stderr 'executable file not found in \$PATH'
