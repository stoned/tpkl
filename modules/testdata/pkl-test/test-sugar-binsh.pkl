amends "pkl:test"

import "tpkl:tpkl"

examples {
  //
  // sugar for running a script with /bin/sh
  //

  ["anonymous-function-with-string"] {
    new tpkl.Task {
      cmds {
        (#"printf '%s\n' "$@""# |> tpkl.binsh) {
          cmd { "-"; "arg1"; "arg 2" }
        }
      }
    }
  }

  ["anonymous-function-with-list"] {
    new tpkl.Task {
      cmds {
        List(#"printf '%s\n' "$@""#, "-", "arg1", "arg 2") |> tpkl.binsh
      }
    }
  }

  ["anonymous-function-with-listing"] {
    new tpkl.Task {
      cmds {
        new Listing { #"printf '%s\n' "$@""#; "-"; "arg1"; "arg 2" } |> tpkl.binsh
      }
    }
  }

  ["anonymous-function-with-dynamic"] {
    new tpkl.Task {
      cmds {
        new Dynamic { #"printf '%s\n' "$@""#; "-"; "arg1"; "arg 2" } |> tpkl.binsh
      }
    }
  }

  ["function-with-string"] {
    new tpkl.Task {
      cmds {
        (tpkl.binsh(#"printf '%s\n' "$@""#)) {
          cmd { "-"; "arg1"; "arg 2" }
        }
      }
    }
  }

  ["function-with-list"] {
    new tpkl.Task {
      cmds {
        tpkl.binsh(List(#"printf '%s\n' "$@""#, "-", "arg1", "arg 2"))
      }
    }
  }

  ["function-with-listing"] {
    new tpkl.Task {
      cmds {
        tpkl.binsh(new Listing { #"printf '%s\n' "$@""#; "-"; "arg1"; "arg 2" })
      }
    }
  }

  ["function-with-dynamic"] {
    new tpkl.Task {
      cmds {
        tpkl.binsh(new Dynamic { #"printf '%s\n' "$@""#; "-"; "arg1"; "arg 2" })
      }
    }
  }

  ["amending"] {
    new tpkl.Task {
      cmds {
        (tpkl.Binsh) {
          cmd { #"printf '%s\n' "$@""#; "-"; "arg1"; "arg 2" }
        }
      }
    }
  }
}
