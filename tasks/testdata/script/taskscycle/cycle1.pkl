import "tpkl:tpkl"
tasks: tpkl.Tasks = new {
  // *cycle*
  //
  //    A --> B
  //    ^     |
  //    |     |
  //    + ----+
  ["A"] { cmds { new { task = "B" } } }
  ["B"] { cmds { new { task = "A" } } }
}
