import "tpkl:tpkl"
tasks: tpkl.Tasks = new {
  ["test"] {
    cmds {
      """
      set -euo pipefail
      basename "${TPKL_FILE_afile}"
      cat "${TPKL_FILE_afile}"
      """ |> tpkl.sh
    }
    files {
      ["afile"] {
        filename = "afile.txt"
        content = "Hello world!\n"
      }
    }
  }
}
