# yaml-language-server: $schema=https://goreleaser.com/static/schema.json
# vim: set ts=2 sw=2 tw=0 fo=cnqoj
version: 2
project_name: tpkl
before:
  hooks:
  - go generate ./...
  - go test ./...
builds:
- id: tpkl
  env:
  - CGO_ENABLED=0
  ldflags:
  - -s -w
  goos:
  - linux
  goarch:
  - amd64
